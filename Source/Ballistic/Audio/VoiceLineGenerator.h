#ifndef VOICELINEGENERATOR_H\n#define VOICELINEGENERATOR_H\n\n#include <string>\n#include <iostream>\n#include <curl/curl.h>\n\nclass VoiceLineGenerator {\npublic:  \n    VoiceLineGenerator(const std::string& apiKey); \n    void generateVoiceLine(const std::string& text);\n\nprivate:  \n    std::string apiKey;\n    static size_t WriteCallback(void* contents, size_t size, size_t nmemb, void* userp);\n};\n\n#endif // VOICELINEGENERATOR_H